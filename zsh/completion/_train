#compdef train.py
_train() {
	_arguments \
	  "1::command:(train_yccp)" \
	  "2::yaml to use:->files" && return 0
	# '*::command: _normal'
	# '*::args:->command' "$common[@]" && ret=0
	case $state in
		files)
			_files -g "*.yaml"
			;;
	esac
	return
}
_train "$@"


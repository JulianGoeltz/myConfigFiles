# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id xterm
#bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
#bindsym $mod+semicolon focus right

# # alternatively, you can use the cursor keys:
# bindsym $mod+Left focus left
# bindsym $mod+Down focus down
# bindsym $mod+Up focus up
# bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+g split h
# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+f fullscreen toggle global

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize/move" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt

        # same bindings, but for the arrow keys
        #bindsym Left resize shrink width 10 px or 10 ppt
        #bindsym Down resize grow height 10 px or 10 ppt
        #bindsym Up resize shrink height 10 px or 10 ppt
        #bindsym Right resize grow width 10 px or 10 ppt
        bindsym Left move left
	bindsym Right move right
	bindsym Up move up
	bindsym Down move down

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize/move"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#     status_command i3status
# }
bar {
        # i3bar_command ~/.config/i3/i3status.sh | lemonbar -p -B "#2e3436" -F "#d3d7cf"
        # status_command ~/.config/i3/i3status.sh | lemonbar -p -B "#2e3436" -F "#d3d7cf"
        #status_command ~/.config/i3/i3status.sh
        # status_command $HOME/.config/i3/conky-i3bar
	i3bar_command /usr/bin/i3bar 
        status_command ~/.config/i3/i3status.sh
	font pango: Awesome 9
        tray_output eDP1
	position top
	colors {
		background #2e3436
		statusline #d3d7cf
	}
}
#exec python /home/julgoe/venvs/py2nest/lib/python2.7/site-packages/powerline/bindings/lemonbar/powerline-lemonbar.py –i3
# bar {
#     status_command python /home/julgoe/venvs/py2nest/lib/python2.7/site-packages/powerline/bindings/i3/powerline-i3.py
#     font pango:PowerlineFont 12
# }

# ###### start of all the new stuff


# bindsym $mod+p exec --no-startup-id "notify-send Hello, i3"
# bindsym $mod+d exec rofi \
# 	-font "pango:monospace 12" \
# 	-opacity 70 \
# 	-width 100 -lines 10 \
# 	-padding 480 -location 0 \
# 	-show run
# 	#-fuzzy \
# 	#-modi drun -show drun
bindsym $mod+d exec --no-startup-id rofi \
	-font "pango:monospace 12" \
	-opacity 70 \
	-width 100 -lines 10 \
	-padding 400 \
	-location 0 \
	-show run
	#-fuzzy \
	#-modi drun -show drun

# set $bg-color #2f343f
# set $text-color #f3f4f5
# bindsym $mod+Shift+a exec rofi -show run -lines 21 -eh 1.3 -opacity "50" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$text-color" -hlfg "$bg-color" -font "System San Francisco Display 10"

# brightness
bindsym XF86MonBrightnessUp "exec --no-startup-id /bin/sh ~/.config/i3/scripts/changeBrightness.sh up"
bindsym XF86MonBrightnessDown "exec --no-startup-id /bin/sh ~/.config/i3/scripts/changeBrightness.sh down"
bindsym Shift+XF86MonBrightnessUp "exec --no-startup-id /bin/bash ~/.config/i3/scripts/changeBrightness.sh high"
bindsym Shift+XF86MonBrightnessDown "exec --no-startup-id /bin/sh ~/.config/i3/scripts/changeBrightness.sh low"

# playpause
bindsym XF86Tools "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh play-pause"
bindsym Shift+XF86Tools "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh next"
bindsym Mod1+XF86Tools "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh previous"
bindsym XF86HomePage "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh play-pause"
bindsym KP_Divide "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh play-pause"
bindsym KP_Subtract "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh next"
bindsym KP_Multiply "exec --no-startup-id /bin/sh ~/.config/i3/scripts/playpausetoggle.sh previous"

# increase correct volume: if bluetooth connected use that one
bindsym XF86AudioLowerVolume "exec --no-startup-id pactl set-sink-volume $(/bin/sh ~/.config/i3/scripts/correctSinkForChangingVolume.sh) -10%"
bindsym XF86AudioRaiseVolume "exec --no-startup-id pactl set-sink-volume $(/bin/sh ~/.config/i3/scripts/correctSinkForChangingVolume.sh) +10%"
bindsym Control+KP_Subtract  "exec --no-startup-id pactl set-sink-volume $(/bin/sh ~/.config/i3/scripts/correctSinkForChangingVolume.sh) -10%"
bindsym Control+KP_Add "exec --no-startup-id pactl set-sink-volume $(/bin/sh ~/.config/i3/scripts/correctSinkForChangingVolume.sh) +10%"
bindsym XF86AudioMute "exec --no-startup-id pactl set-sink-mute $(/bin/sh ~/.config/i3/scripts/correctSinkForChangingVolume.sh) toggle"
bindsym $mod+m "exec --no-startup-id /bin/sh ~/.config/i3/scripts/muteAll.sh"

# syncient

# background
# preparing image: convert IMG_1820.JPG -resize 1920 test.png; convert test.png -crop 1920x1080+0+0 test.png
# or better yet with http://www.fmwconcepts.com/imagemagick/aspectcrop/index.php
#exec_always --no-startup-id "feh -z --bg-max --auto-rotate /home/julgoe/Pictures/BackgroundCropped"
# and transparency
exec --no-startup-id compton -bc
# for locking: 
#bindsym $mod+Mod1+l exec "i3lock -befi /home/julgoe/Pictures/actualBackgrounds/lock_1820.png -c 000000"
# used i3lock is not standard but this one:
# https://github.com/PandorasFox/i3lock-color
# exec --no-startup-id xautolock -detectsleep -time 30 -locker "/home/julgoe/myConfigFiles/toolbox/lock.sh"
# bindsym $mod+Mod1+Shift+l exec xautolock -detectsleep -time 30 -locker "/home/julgoe/myConfigFiles/toolbox/lock.sh"
bindsym $mod+Mod1+l exec --no-startup-id "/home/julgoe/myConfigFiles/toolbox/lock.sh"
bindsym $mod+Mod1+Shift+l exec --no-startup-id "/home/julgoe/myConfigFiles/toolbox/lockAlt.sh"

# autostart
# find classes with xprop: WM_CLASS: class, instance; 
for_window [class="Spotify"] move --no-auto-back-and-forth to workspace 10
assign [class="KeePass2"] 10
assign [class="TelegramDesktop"] 9
exec --no-startup-id i3-msg 'workspace 1; exec xterm'
exec --no-startup-id /usr/bin/keepass
exec --no-startup-id workspace 1
exec --no-startup-id xterm -e "cd /home/julgoe/Documents/vimwiki/text && zsh -c 'tmux attach || tmux'"

bindsym $mod+Shift+v exec --no-startup-id xterm -e "cd /home/julgoe/Documents/vimwiki/text && zsh -c 'tmux attach || tmux'"
for_window [ title="julgoe::T2" ] move scratchpad


# switch to next/prev window
# bindsym $mod+Left exec i3-msg "workspace $(( ($(i3-msg -t get_workspaces | jq '.[] | select(.focused==true).num')-1-1)%10+1))"
# bindsym $mod+Right exec i3-msg "workspace $(( ($(i3-msg -t get_workspaces | jq '.[] | select(.focused==true).num')+1-1)%10+1))"
bindsym $mod+Left workspace prev
bindsym $mod+Right workspace next

# status bar
# exec python /usr/local/bin/powerline/bindings/lemonbar/powerline-lemonbar.py –i3

# dont focus on window for each mouse hover
focus_follows_mouse no

# touchpad: fusuma and other
# sudo gem install --install-dir=/usr/local --no-user-install fusuma
# or better yay fusuma
exec --no-startup-id fusuma
exec_always --no-startup-id i3-msg 'exec synclient ClickFinger3=2; exec synclient TapButton3=2; exec synclient HorizTwoFingerScroll=1'

# dunst notification daemon for /usr/bin/notify-send
exec --no-startup-id i3-msg 'exec dunst; exec echo "running" > /home/julgoe/.tmp_dunststate'
bindsym $mod+p exec --no-startup-id "~/.config/i3/scripts/dunstToggle.sh pause"
bindsym $mod+Shift+p exec --no-startup-id "~/.config/i3/scripts/dunstToggle.sh resume"

# back and forth
workspace_auto_back_and_forth yes
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+q move scratchpad
bindsym $mod+Escape scratchpad show

# moving workspaces to other output
bindsym $mod+o move workspace to output right

# maybe use udiskie even in autostart to automaticcaly mount devices
# exec --no-startup-id i3-msg 'exec udiskie --tray'

# opening nemo for easy folder management
#bindsym $mod+e exec nemo

#always make inkviews floating
# find classes with xprop: WM_CLASS: class, instance; 
for_window [class="Inkview" instance="inkview"] floating enable
#for_window [class="vlc"] floating enable # leave like this, then we can compare videos better
for_window [class="Xviewer"] floating enable
for_window [class="Xplayer"] floating enable
for_window [title="Figure *"] floating enable
for_window [class="Eom"] floating enable
for_window [class="Eog"] floating enable
for_window [class="feh"] floating enable

# bind key to use screenSet, easiest for making screen work after removing from dockingstation
bindsym $mod+Control+1 "exec --no-startup-id /bin/sh ~/myConfigFiles/toolbox/screenSet.sh"

# bind key for kaddl
bindsym Mod1+F4 exec --no-startup-id "notify-send -t 2000 'Hallo Kaddl ❤❤❤'"

#bind key for disabling touchpad
bindsym $mod+t "exec --no-startup-id /bin/sh ~/myConfigFiles/i3scripts/toggleTouchpad.sh"

# for easy network managment
exec --no-startup-id nm-applet

# proxied telegram
bindsym Mod1+t exec --no-startup-id "/bin/bash ~/myConfigFiles/toolbox/proxies.sh proxify /usr/bin/telegram-desktop"

# netcat listening for slurmDone calls
exec --no-startup-id i3-msg "exec ~/myConfigFiles/toolbox/notifySlurmDone.sh receiving"

# enable tapping 
exec --no-startup-id i3-msg "exec xinput set-prop 'SynPS/2 Synaptics TouchPad' 'libinput Tapping Enabled' 1"

# disable caps lock and use as additional ctrl 
exec --no-startup-id i3-msg "exec  setxkbmap -option ctrl:nocaps"

# use rofi for selecting unicode symbols as eg umlauts
bindsym $mod+u exec --no-startup-id "exec /bin/sh ~/myConfigFiles/i3scripts/rofiUnicodeSymbols.sh"

# use rofi as window switcher
bindsym $mod+w exec --no-startup-id rofi -show window

# floating temporary terminal
for_window [class="FXTerm"] floating enable, move right 1500, move down 800
# resize grow left 300, resize grow right 300, 
bindsym $mod+Shift+Return exec --no-startup-id xterm -class FXTerm

# window setting without borders
default_border none
default_floating_border none
hide_edge_borders both

# jump to urgent window
bindsym $mod+x [urgent=latest] focus

# make window sticky
bindsym $mod+s sticky toggle
